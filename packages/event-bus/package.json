{
  "name": "@render/event-bus",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "tsc -p tsconfig.json && node --test dist/eventBus.test.js"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^22.10.7"
  }
}
